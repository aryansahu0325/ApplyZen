# ApplyZen - Python AI Service Production Dockerfile
# Python-based multi-agent service:
# Uses Playwright-enabled base image (or python-slim with playwright dependencies), installs pip requirements, and exposes FastAPI ports.
